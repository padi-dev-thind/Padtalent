export enum GameType {
    logical = 1,
    memory = 2,
  }

export enum GameTotalTime {
    logical = 90,
    memory = 300,
  }

export enum NumberOfQuestionGame {
    logical = 20,
    memory = 25,
  }
  

  